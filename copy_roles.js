javascript:(function(){    const role = document.querySelector('h1.t-24.t-bold.inline a')?.innerText.trim();    const company = document.querySelector(%27div.job-details-jobs-unified-top-card__company-name a%27)?.innerText.trim();    if (role && company) {        const entry = { role, company };        const existing = JSON.parse(localStorage.getItem(%27linkedinJobs%27) || %27[]%27);        existing.push(entry);        localStorage.setItem(%27linkedinJobs%27, JSON.stringify(existing));        alert("Saved to memory:\n" + role + " at " + company);    } else {        alert("Could not find role or company on this page.");    }})();